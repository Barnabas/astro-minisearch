{
    "name": "astro-minisearch-monorepo",
    "private": true,
    "scripts": {
        "build": "pnpm -r --filter='./packages/*' run build"
    },
    "devDependencies": {
        "@changesets/cli": "^2.24.4"
    }
}